"""
TUI Package - Terminal User Interface layer

Provides rich, human-friendly terminal output for quantitative analysis.
All data is fetched via ``quant.skills`` — this package only handles
argument parsing and result formatting.
"""
